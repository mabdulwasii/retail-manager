# These values are only used as fallbacks if window.RUNTIME_CONFIG is not available
# In Kubernetes deployments, config.js from ConfigMap will override these
VITE_API_BASE_URL=
VITE_KEYCLOAK_URL=
VITE_KEYCLOAK_REALM=
VITE_KEYCLOAK_CLIENT_ID=
VITE_APP_VERSION=1.0.0
VITE_APP_ENVIRONMENT=kubernetes
