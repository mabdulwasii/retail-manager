global:
  appName: "retail"
  domain: "gomco.com"

# Use manual auth - Keycloak only initializes when user clicks Login
frontend:
  image:
    tag: "manual-auth"

branding:
  platformName: "GOMCO Retail Pro"
  companyName: "GOMCO Corporation"
  platformDescription: "Advanced Retail Management System"
  colors:
    primary: "#2E7D32"
    secondary: "#FF6F00"

# TLS Configuration
tls:
  enabled: true
  issuer: "local-ca-issuer"  # Use local-ca-issuer for local development
  email: "admin@gomco.com"
  localCertInstallation:
    enabled: true

# Disable existingSecret - let Helm create secrets
postgresql:
  auth:
    existingSecret: ""

minio:
  auth:
    existingSecret: ""