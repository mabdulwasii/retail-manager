# ============================================================================
# Shop Manager - Windows Installation Spring Boot Configuration
# ============================================================================
# This file overrides defaults from the embedded JAR's application-embedded.yml
# Most configuration is already in the JAR - only override what's needed
# ============================================================================

# Server Configuration
server:
  port: ${BACKEND_PORT:80}

# Logging Configuration
logging:
  file:
    name: ./data/logs/shop-manager.log
